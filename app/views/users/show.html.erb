<div class="row justify-content-center">
  <div class="col-6">
    <h1>Profile Information</h1>
    <div class="mb-3">
      <h4>First Name</h4>
      <%= current_user.first_name %>
    </div>
    <div class="mb-3">
      <h4>Last Name</h4>
      <%= current_user.last_name %>
    </div>
    <div class="mb-3">
      <h4>Email</h4>
      <%= current_user.email %>
    </div>
    <div class="mb-3">
      <h4>Birthdate</h4>
      <%= current_user.birthdate.strftime("%B %e, %Y") %>
    </div>
    <div class="mb-3">
      <h4>Gender</h4>
      <%= current_user.gender %>
    </div>
    <div class="mb-3">
      <h4>Phone Number</h4>
      <%= number_to_phone(current_user.phone_number) %>
    </div>
    <div class="mb-3">
      <%= link_to "Edit", edit_user_path(current_user), class: 'btn btn-primary' %>
    </div>
  </div>
</div>